// Чему будут равны переменные a, b, c и d в примере ниже?
let a = 1, b = 1; // значение 1 и там, и там

let c = ++a; // 2 значение с уже инкрементировано
let d = b++; // 1 значение d еще не инкрементироано
// 2 значение а, b после инкрементаций
